# @generated by @hopdrive/eventkit/flow — DO NOT EDIT BY HAND.
# Regenerate with:  npx eventkit-flow generate --kit <module> --out <file>
# CI drift check:   npx eventkit-flow check    --kit <module> --out <file>
#
# Documents the STRUCTURE of events registered on the kit (source -> event -> jobs),
# derived from the declarative modules. Business MEANING (cross-event chains, intent)
# belongs in a hand-authored flow manifest — see docs (§15).
generator: "@hopdrive/eventkit/flow"
schema: 1
title: db-rideshares
kit:
  source:
    name: source-hasura-event
    type: database
  platform: platform-netlify
  plugins: [loop-guard, correlation-resolver]
events:
  - name: rideshare.requested
    response: none
    description: A rideshare was requested for a move
    owner: mobility
    flowHints:
      flow: uber-ride
    jobs:
      - name: callVendorForRide
        metadata:
          sideEffect: uber-api
      - name: notifyDispatcher
        retries: 2
  - name: vendor.ride.driver_assigned
    response: none
    jobs:
      - name: updateRidehailSystem
        continueOnFailure: false
