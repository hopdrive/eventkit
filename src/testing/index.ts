// =============================================================================
// @hopdrive/eventkit/testing
// =============================================================================
// Test helpers (fake sources, envelope/context builders) for unit-testing
// detectors, handlers, jobs, and plugins (§20). Phase 1 fills this in alongside
// the core runtime; the subpath exists now so the exports map and bundle smoke
// test cover it from the start.

export {};
